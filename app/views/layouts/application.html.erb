<!DOCTYPE html>
<html>
  <head>
    <title>ChatApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body class="bg-gray-100">
    <!-- Flash Messages -->
    <p class="notice text-green-500 text-center"><%= notice %></p>
    <p class="alert text-red-500 text-center"><%= alert %></p>

    <!-- Navbar -->
    <nav class="bg-gray-900 text-white p-4 shadow-md mt-12" style="margin: 2%;">
      <div class="container mx-auto flex justify-between items-center">
        <!-- Logo / Home -->
        <a href="<%= root_path %>" class="text-xl font-bold text-black hover:text-yellow text-white">
          🏠 Home
        </a>
        
        <a href="<%= group_room_path(1) %>" class="text-xl font-bold text-black hover:text-yellow text-white">
          Group Chat
        </a>

        <!-- Navigation Links -->
        <div class="space-x-6">
       

          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-red-500 px-4 py-2 rounded-lg hover:bg-red-700 transition" %>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto px-5">
      <%= yield %>
    </main>
  </body>
</html>
